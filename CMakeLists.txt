CMAKE_MINIMUM_REQUIRED(VERSION 3.0)

PROJECT(steiner)

SET(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -O3 -std=c++11")

SET(BASTPATH ${CMAKE_SOURCE_DIR})
INCLUDE_DIRECTORIES(${BASEPATH})
ADD_EXECUTABLE(steiner
    src/main.cpp
    src/Steiner.cpp
)
